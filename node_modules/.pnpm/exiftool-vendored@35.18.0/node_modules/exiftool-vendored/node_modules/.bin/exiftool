#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -z "$NODE_PATH" ]; then
  export NODE_PATH="/Users/zhashuo/Projects/PokerGame/node_modules/.pnpm/exiftool-vendored.pl@13.57.0/node_modules/exiftool-vendored.pl/node_modules:/Users/zhashuo/Projects/PokerGame/node_modules/.pnpm/exiftool-vendored.pl@13.57.0/node_modules:/Users/zhashuo/Projects/PokerGame/node_modules/.pnpm/node_modules"
else
  export NODE_PATH="/Users/zhashuo/Projects/PokerGame/node_modules/.pnpm/exiftool-vendored.pl@13.57.0/node_modules/exiftool-vendored.pl/node_modules:/Users/zhashuo/Projects/PokerGame/node_modules/.pnpm/exiftool-vendored.pl@13.57.0/node_modules:/Users/zhashuo/Projects/PokerGame/node_modules/.pnpm/node_modules:$NODE_PATH"
fi
if [ -x "$basedir/perl" ]; then
  exec "$basedir/perl"  "$basedir/../../../exiftool-vendored.pl/bin/exiftool" "$@"
else
  exec perl  "$basedir/../../../exiftool-vendored.pl/bin/exiftool" "$@"
fi
